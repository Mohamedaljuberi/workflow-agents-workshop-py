projects:
- name: mohamedaljuberi-agents-workshop-naive
  environments:
  - name: production
    databases:
    - name: mohamedaljuberi-naive-agent-db
      plan: basic-256mb
      region: oregon
      postgresMajorVersion: '18'
    services:
    - type: web
      name: mohamedaljuberi-naive-agent
      runtime: python
      region: oregon
      plan: starter
      buildCommand: pip install uv && uv sync --frozen --no-dev --package workshop-naive-agent
      startCommand: uv run python -m naive_agent.server
      healthCheckPath: /healthz
      envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mohamedaljuberi-naive-agent-db
          property: connectionString
      - key: PYTHON_VERSION
        value: '3.12'
