projects:
- name: mohamedaljuberi-agents-workshop-queue
  environments:
  - name: production
    databases:
    - name: mohamedaljuberi-queue-agents-db
      plan: basic-256mb
      region: oregon
      postgresMajorVersion: '18'
    services:
    - type: keyvalue
      name: mohamedaljuberi-queue-agents-valkey
      plan: starter
      region: oregon
      maxmemoryPolicy: noeviction
      ipAllowList: []
    - type: web
      name: mohamedaljuberi-queue-agents-web
      runtime: python
      region: oregon
      plan: starter
      buildCommand: pip install uv && uv sync --frozen --no-dev --package workshop-queue-agents
      startCommand: uv run python -m queue_agents.web
      healthCheckPath: /healthz
      envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mohamedaljuberi-queue-agents-db
          property: connectionString
      - key: VALKEY_URL
        fromService:
          name: mohamedaljuberi-queue-agents-valkey
          type: keyvalue
          property: connectionString
      - key: PYTHON_VERSION
        value: '3.12'
    - type: worker
      name: mohamedaljuberi-queue-agents-worker
      runtime: python
      region: oregon
      plan: starter
      numInstances: 1
      buildCommand: pip install uv && uv sync --frozen --no-dev --package workshop-queue-agents
      startCommand: uv run python -m queue_agents.worker
      envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mohamedaljuberi-queue-agents-db
          property: connectionString
      - key: VALKEY_URL
        fromService:
          name: mohamedaljuberi-queue-agents-valkey
          type: keyvalue
          property: connectionString
      - key: PYTHON_VERSION
        value: '3.12'
